<Window x:Class="LauncherTF2.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Width="400" MinHeight="160" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource PageBackgroundBrush}"
        KeyDown="Window_KeyDown" ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Grid.Row="0" Foreground="{DynamicResource TextBrush}" TextWrapping="Wrap" Margin="0,0,0,10"/>
        
        <TextBox x:Name="InputTextBox" Grid.Row="1" Height="28" Padding="4" Margin="0,0,0,10"
                 Background="{DynamicResource SurfaceInsetBrush}" Foreground="{DynamicResource TextBrush}"
                 BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>

        <TextBlock x:Name="DescriptionLabel" Grid.Row="2" Text="Description (optional):" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4"/>
        
        <TextBox x:Name="DescriptionTextBox" Grid.Row="3" Height="60" Padding="4" TextWrapping="Wrap" AcceptsReturn="True"
                 Background="{DynamicResource SurfaceInsetBrush}" Foreground="{DynamicResource TextBrush}"
                 BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>

        <DockPanel Grid.Row="5" LastChildFill="False" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel" Click="CancelButton_Click" Width="80" Padding="8,4" Margin="0,0,10,0" Style="{StaticResource ModernButtonStyle}"/>
            <Button Content="OK" Click="OkButton_Click" Width="80" Padding="8,4" Background="{DynamicResource AccentBrush}" Foreground="White" Style="{StaticResource ModernButtonStyle}"/>
        </DockPanel>
    </Grid>
</Window>
