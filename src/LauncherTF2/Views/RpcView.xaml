<UserControl x:Class="LauncherTF2.Views.RpcView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:LauncherTF2.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1030">
    <UserControl.Resources>
        <!-- Styles copied/adapted from SettingsView for consistency -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent" Cursor="Hand">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Border x:Name="SwitchTrack" Grid.Column="1" Width="40" Height="20" CornerRadius="10" Margin="10,0,0,0">
                                <Border.Background>
                                    <SolidColorBrush x:Name="TrackBrush" Color="#333"/>
                                </Border.Background>
                                <Grid>
                                    <Ellipse x:Name="SwitchKnob" Width="16" Height="16" HorizontalAlignment="Left" Margin="2,0,0,0">
                                        <Ellipse.Fill>
                                            <SolidColorBrush x:Name="KnobBrush" Color="#888"/>
                                        </Ellipse.Fill>
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="KnobTransform" X="0"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                </Grid>
                            </Border>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="KnobTransform" Storyboard.TargetProperty="X" To="20" Duration="0:0:0.2" DecelerationRatio="0.5"/>
                                            <ColorAnimation Storyboard.TargetName="TrackBrush" Storyboard.TargetProperty="Color" To="#ff6b00" Duration="0:0:0.2"/>
                                            <ColorAnimation Storyboard.TargetName="KnobBrush" Storyboard.TargetProperty="Color" To="White" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="KnobTransform" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.2" DecelerationRatio="0.5"/>
                                            <ColorAnimation Storyboard.TargetName="TrackBrush" Storyboard.TargetProperty="Color" To="#333" Duration="0:0:0.2"/>
                                            <ColorAnimation Storyboard.TargetName="KnobBrush" Storyboard.TargetProperty="Color" To="#888" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="#ddd"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Discord Rich Presence" FontSize="32" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>



        <!-- Status Panel -->
        <Border Grid.Row="2" Background="#222" CornerRadius="10" Padding="20" Margin="0,0,0,20">
            <StackPanel>
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="12" Height="12" Fill="{Binding StatusColor}" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding StatusText}" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Start RPC" Command="{Binding StartRpcCommand}" Style="{StaticResource ModernButtonStyle}" Background="#2e7d32" Margin="0,0,10,0"/>
                        <Button Content="Stop RPC" Command="{Binding StopRpcCommand}" Style="{StaticResource ModernButtonStyle}" Background="#c62828"/>
                    </StackPanel>
                </Grid>
                
                <Separator Background="#333" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Current Map" Foreground="#aaa" Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding CurrentMap}" Foreground="White" FontSize="18"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Queue Status" Foreground="#aaa" Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding QueueStatus}" Foreground="White" FontSize="18"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Settings -->
        <Border Grid.Row="3" Background="#222" CornerRadius="10" Padding="20" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Settings" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                
                <Grid Margin="0,0,0,10">
                    <TextBlock Text="Auto-start RPC with Launcher" Foreground="#ddd" VerticalAlignment="Center"/>
                    <CheckBox IsChecked="{Binding AutoStartRpc}" Style="{StaticResource ToggleSwitchStyle}" HorizontalAlignment="Right"/>
                </Grid>
                
                <Grid Margin="0,0,0,10">
                    <TextBlock Text="Auto-start when TF2 is detected" Foreground="#ddd" VerticalAlignment="Center"/>
                    <CheckBox IsChecked="{Binding AutoStartWhenGameDetected}" Style="{StaticResource ToggleSwitchStyle}" HorizontalAlignment="Right"/>
                </Grid>
                
                <Grid>
                    <TextBlock Text="Pause RPC when TF2 closes" Foreground="#ddd" VerticalAlignment="Center"/>
                    <CheckBox IsChecked="{Binding PauseWhenGameCloses}" Style="{StaticResource ToggleSwitchStyle}" HorizontalAlignment="Right"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
