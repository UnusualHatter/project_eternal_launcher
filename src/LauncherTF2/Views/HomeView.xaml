<UserControl x:Class="LauncherTF2.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1030">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Background/Banner Area -->
        <!-- Background Images for Transition -->
        <Image x:Name="BackgroundImage1" Grid.RowSpan="2" Source="/Resources/Assets/backrnd1.png" Stretch="UniformToFill"/>
        <Image x:Name="BackgroundImage2" Grid.RowSpan="2" Opacity="0" Stretch="UniformToFill"/>

        <!-- Content Overlay -->
        <Grid Grid.RowSpan="2">
            <StackPanel Margin="50,80,50,0">
                <TextBlock Text="TEAM FORTRESS 2" FontSize="60" FontWeight="Black" Foreground="White" TextWrapping="Wrap">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="5" Opacity="0.5"/>
                    </TextBlock.Effect>
                </TextBlock>
                
                <StackPanel Orientation="Horizontal" Margin="0,10">
                    <TextBlock Text="★★★★★" Foreground="#ffc107" FontSize="16"/>
                    <TextBlock Text="Top Seller" Foreground="#aaa" Margin="10,0" VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Text="Nine distinct classes provide a broad range of tactical abilities and personalities in this team-based first-person shooter." 
                           Foreground="#ddd" FontSize="16" TextWrapping="Wrap" MaxWidth="600" Margin="0,20"/>

                <Button Content="FREE TO PLAY" Command="{Binding PlayCommand}" 
                        Style="{StaticResource ModernButtonStyle}" HorizontalAlignment="Left" Width="200"/>
            </StackPanel>
        </Grid>

        <!-- Bottom Cards -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="50,0,50,50" VerticalAlignment="Bottom">
            <!-- Card 1 -->
            <Border Background="{StaticResource CardBrush}" Width="300" Height="180" CornerRadius="10" Margin="0,0,20,0">
                <Grid>
                    <TextBlock Text="Premium Account" Foreground="White" VerticalAlignment="Bottom" Margin="15" FontWeight="Bold"/>
                    <TextBlock Text="Upgrade Now" Foreground="#aaa" VerticalAlignment="Bottom" Margin="15,35" FontSize="12"/>
                </Grid>
            </Border>
            
            <!-- Card 2 -->
            <Border Background="{StaticResource CardBrush}" Width="300" Height="180" CornerRadius="10">
                <Grid>
                    <TextBlock Text="The Orange Box" Foreground="White" VerticalAlignment="Bottom" Margin="15" FontWeight="Bold"/>
                    <TextBlock Text="Includes TF2" Foreground="#aaa" VerticalAlignment="Bottom" Margin="15,35" FontSize="12"/>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
