<UserControl x:Class="LauncherTF2.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1030">
    <UserControl.Resources>
        <Style x:Key="DashboardCardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform Y="14"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CardShell"
                                Background="{TemplateBinding Background}"
                                BorderBrush="#2f2f35"
                                BorderThickness="1"
                                CornerRadius="16"
                                Padding="0"
                                SnapsToDevicePixels="True">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.28"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.28"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CardShell" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="CardShell" Property="Background" Value="#313138"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="10"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MicroLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Resources/Assets/backrnd1.png" Stretch="UniformToFill" Opacity="0.14"/>
        </Grid.Background>

        <Border Background="#cf0f1014"/>

        <Grid RenderTransformOrigin="0.5,0.5">
            <Grid.RenderTransform>
                <TranslateTransform Y="12"/>
            </Grid.RenderTransform>
            <Grid.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.35"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="12" To="0" Duration="0:0:0.35"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Grid.Triggers>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{StaticResource ModernScrollViewerStyle}">
                <StackPanel Margin="28,22,28,28">
                    <Grid Margin="0,0,0,22">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" MaxWidth="680">
                            <TextBlock Text="TEAM FORTRESS 2" FontSize="54" FontWeight="Black" Foreground="White" TextWrapping="Wrap">
                                <TextBlock.Effect>
                                    <DropShadowEffect BlurRadius="18" ShadowDepth="6" Opacity="0.45"/>
                                </TextBlock.Effect>
                            </TextBlock>

                            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                                <Border Background="#22ff6b00" CornerRadius="999" Padding="10,4" Margin="0,0,10,0">
                                    <TextBlock Text="LIVE DASHBOARD" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold" FontSize="11"/>
                                </Border>
                                <TextBlock Text="Steam news and fresh GameBanana mods, pulled in real time." Foreground="#cfcfd6" VerticalAlignment="Center"/>
                            </StackPanel>

                            <TextBlock Text="Track TF2 updates, skim announcements, and browse the newest mod releases without leaving the launcher." Foreground="#d7d7de" FontSize="15" TextWrapping="Wrap" MaxWidth="600" Margin="0,18,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20,10,0,0">
                            <Button Content="LAUNCH TF2" Command="{Binding PlayCommand}" Style="{StaticResource ModernButtonStyle}" Padding="22,12" FontSize="15" Margin="0,0,0,10"/>
                            <Button Content="REFRESH FEED" Command="{Binding RefreshCommand}" Style="{StaticResource ModernButtonStyle}" Padding="18,10" Background="#2f3136" FontSize="12"/>
                        </StackPanel>
                    </Grid>

                    <Border Background="{StaticResource CardBrush}" CornerRadius="18" Padding="20" Margin="0,0,0,20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel>
                                    <TextBlock Text="TF2 Update Feed" FontSize="20" FontWeight="SemiBold" Foreground="White"/>
                                    <TextBlock Text="Latest Steam changelogs and announcements" Style="{StaticResource MicroLabelStyle}" Margin="0,2,0,0"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" Text="Steam API" VerticalAlignment="Center" Foreground="#9a9aa3" FontSize="11"/>
                            </Grid>

                            <Grid Grid.Row="1">
                                <ItemsControl ItemsSource="{Binding NewsItems}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button Style="{StaticResource DashboardCardButtonStyle}"
                                                    Command="{Binding DataContext.OpenUrlCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding Url}"
                                                    Margin="0,0,0,12">
                                                <Border Background="#1c1c20" CornerRadius="14" Padding="18">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>

                                                        <Border Width="48" Height="48" CornerRadius="12" Background="#2a2a31" VerticalAlignment="Top" Margin="0,0,14,0">
                                                            <TextBlock Text="📰" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>

                                                        <StackPanel Grid.Column="1">
                                                            <DockPanel Margin="0,0,0,6">
                                                                <TextBlock Text="{Binding Title}" Foreground="White" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" DockPanel.Dock="Left" Margin="0,0,14,0"/>
                                                                <Border DockPanel.Dock="Right" Background="#22ff6b00" CornerRadius="999" Padding="8,3" VerticalAlignment="Top">
                                                                    <TextBlock Text="{Binding FeedLabel}" Foreground="{StaticResource AccentBrush}" FontSize="10" FontWeight="SemiBold"/>
                                                                </Border>
                                                            </DockPanel>
                                                            <TextBlock Text="{Binding DateFormatted}" Foreground="#a6a6af" FontSize="11" Margin="0,0,0,8"/>
                                                            <TextBlock Text="{Binding Contents}" Foreground="#d7d7de" TextWrapping="Wrap" MaxHeight="56" TextTrimming="CharacterEllipsis"/>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <Border Background="#17171b" CornerRadius="14" Padding="16" HorizontalAlignment="Center" VerticalAlignment="Center"
                                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Loading live TF2 news..." Foreground="#d0d0d7" Margin="0,0,10,0"/>
                                        <ProgressBar Width="120" Height="6" IsIndeterminate="True"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>

                    <Border Background="{StaticResource CardBrush}" CornerRadius="18" Padding="20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel>
                                    <TextBlock Text="New TF2 Mods on GameBanana" FontSize="20" FontWeight="SemiBold" Foreground="White"/>
                                    <TextBlock Text="Fresh releases with thumbnails and author names" Style="{StaticResource MicroLabelStyle}" Margin="0,2,0,0"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" Text="GameBanana API" VerticalAlignment="Center" Foreground="#9a9aa3" FontSize="11"/>
                            </Grid>

                            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Style="{StaticResource ModernScrollViewerStyle}">
                                <Grid>
                                    <ItemsControl ItemsSource="{Binding NewMods}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Style="{StaticResource DashboardCardButtonStyle}"
                                                        Command="{Binding DataContext.OpenUrlCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding ProfileUrl}"
                                                        Width="210"
                                                        Margin="0,0,14,0">
                                                    <Border Background="#1c1c20" CornerRadius="16" Padding="12">
                                                        <StackPanel>
                                                            <Border Height="118" CornerRadius="12" Background="#111114" ClipToBounds="True">
                                                                <Grid>
                                                                    <Image Source="{Binding ThumbnailUrl}" Stretch="UniformToFill"/>
                                                                    <Border Background="#33000000" Visibility="{Binding ThumbnailUrl, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}">
                                                                        <TextBlock Text="No preview" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                    </Border>
                                                                </Grid>
                                                            </Border>

                                                            <TextBlock Text="{Binding Name}" Foreground="White" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,12,0,4"/>
                                                            <TextBlock Text="{Binding Author}" Foreground="#b2b2bb" FontSize="11" TextWrapping="Wrap"/>
                                                            <TextBlock Text="{Binding DateFormatted}" Foreground="#8e8e98" FontSize="10" Margin="0,6,0,0"/>
                                                        </StackPanel>
                                                    </Border>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <Border Background="#17171b" CornerRadius="14" Padding="16" HorizontalAlignment="Center" VerticalAlignment="Center"
                                            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="Loading GameBanana mods..." Foreground="#d0d0d7" Margin="0,0,10,0"/>
                                            <ProgressBar Width="120" Height="6" IsIndeterminate="True"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
