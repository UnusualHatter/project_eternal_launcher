<UserControl x:Class="LauncherTF2.Views.ModsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LauncherTF2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1030">
    <UserControl.Resources>
        <d:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </UserControl.Resources>
    <Grid Margin="30">
        <StackPanel>
            <Border Background="{StaticResource CardBrush}" Padding="30" CornerRadius="10">
                <StackPanel>
                    <TextBlock Text="Cukei's Casual Preloader" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Set you tf folder path in the configs before using!" Foreground="#aaa" TextWrapping="Wrap" Margin="0,10,0,20"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBlock Text="Status: " Foreground="#aaa" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>

                        <Button Content="Reset" Command="{Binding ResetPreloaderCommand}" 
                                Style="{StaticResource ModernButtonStyle}" Background="#d32f2f" Width="120" Height="35"/>
                    </StackPanel>

                    <Border x:Name="HostContainer" Background="#1e1e1e" MinHeight="500">
                        <TextBlock Text="Loading Casual Preloader..." HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#888"/>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>

